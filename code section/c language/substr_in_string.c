#include <stdio.h>
int main()
{
    int n,n1;
    printf("Enter the length of string \n");
    scanf("%d",&n);
    printf("Enter the length of sub-string you want to check inside main string\n");
    scanf("%d",&n1);
    int str[n],sbstr[n1];
    gets(str);
    gets(sbstr);
}