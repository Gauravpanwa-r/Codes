#include <stdio.h>
int main()
{
    float radius,area;
    printf("Enter the radius\n");
    scanf("%f",&radius);
    area=3.14*radius*radius;
    printf("area of cirle is %f",area);

    return 0;
}
